# cc-bar 产品需求

> 描述当前已交付版本的功能范围与边界。新需求迭代时以本文为准；与代码不一致以代码为准并同步回这里。

---

## 0. 产品定位

- **形态**：常驻菜单栏的小工具（Accessory App，默认不占 Dock；打开主窗口期间临时显示 Dock 图标，关闭后恢复）。
- **平台**：macOS 14 Sonoma+（依赖 SwiftUI `MenuBarExtra(.window)`、`@Observable`、`SMAppService`）。
- **设计**：以 macOS HIG 为底座，关键信息层（百分比、进度、KPI、HUD）按设计稿落地自绘组件；非关键层（Picker / Toggle / DatePicker / TabView / Material）全部用系统默认。
- **一句话价值**：装上它 → 不打开 CLI 也能随时看到 Codex / Claude Code / Antigravity 的当前额度，以及启用后的 Cursor Total / Auto / API 额度，并查看 Codex / Claude Code / Pi / OpenCode 的本地 Token 与费用；启用 Cursor 统计后，还可查看该 Cursor 账号全设备的服务端计量用量。

---

## 1. 账号自动识别

- 启动时扫描本机已有的 Codex / Claude Code / Antigravity / Cursor / Command Code 凭据：
  - Codex：`~/.codex/auth.json`。支持两种凭据形态：① OAuth 模式（`tokens.*`，解析 JWT 拿邮箱 / plan / accountId，到期自动续期）；② personal access token 模式（顶层 `personal_access_token`，即 ChatGPT Team/Enterprise 后台创建的 Codex 访问令牌，不透明、不续期，身份由 `wham/usage` 响应回填）。导入「其他 Codex 账号」时也接受这两种形态的粘贴。
  - Claude：先读 `~/.claude/.credentials.json`；不存在则读 macOS Keychain 通用密码项 `Claude Code-credentials`（service name）。邮箱兜底从 `~/.claude.json` 顶层 `oauthAccount.emailAddress` 读取。
  - Cursor：只读 `~/Library/Application Support/Cursor/User/globalStorage/state.vscdb` 的 `cursorAuth/accessToken`，解析 JWT 仅用于识别账号；不读取 refresh token、不调用 OAuth、不写 Cursor SQLite 或 cc-bar Keychain。
  - Antigravity：`~/.gemini/jetski-standalone-oauth-token`（优先，Antigravity 生态真源：agy CLI / IDE 插件登录即写此文件）或 `~/.gemini/oauth_creds.json`（兜底，旧版 Gemini CLI 遗留），账号身份由云端 `loadCodeAssist` 响应回填，`refresh_token` 自动续期（双 client 兼容，Cloud Mode 无需本地 IDE）。
  - Command Code：5 级只读自动扫描（`~/.commandcode/auth.json`、Pi `auth.json`、OpenCode `auth.json`、环境变量 `COMMAND_CODE_API_KEY` / `COMMANDCODE_API_KEY`），支持在设置中切换为 macOS Keychain 手动 API Key 存储。
- 不做 App 内登录流程。前提：用户已经在终端跑过 `codex login` / `claude` 登录。
- 用户可在「设置 → 账号」勾选要显示的额度 Provider；Cursor 与 Command Code 的 Provider 开关默认关闭，启用后才查询远端接口。Command Code 作为订阅额度 Provider，进入「设置 → 账号」、Popover、菜单栏与桌面悬浮窗；不进入主窗口用量统计。
- 首次启动若不存在 `~/.claude/.credentials.json`，且尚未提示过，会先弹一次说明性 Alert（"接下来 macOS 会弹 Keychain 授权窗口，请选择始终允许"），再触发系统 Keychain 弹窗。

---

## 2. 菜单栏标签

- 标签是模板图像（template NSImage），自适应深浅菜单栏。
- 顺序固定：**Codex → Claude Code → Antigravity → Cursor → Command Code**。
- 每段为 `<服务 logo>  <百分比>`：
  - Logo 用项目内置的 `codex.svg` / `claude.svg` / `cursor.svg` / `commandcode.svg`（菜单栏会做模板化变灰）。
  - 百分比用 `monospacedDigit`。
- 设置项：
  - 显示哪个服务（Codex / Claude Code / Cursor / Command Code，可分别开关）。Cursor 始终只显示 Total；Auto / API 详见 Popover，不能被伪装为周额度。Command Code 支持 5 小时与周额度。
  - 显示哪个额度窗口：主要额度 / 周窗口 / 两者。主要额度通常是 5 小时；服务端临时只返回周额度时自动显示周额度。两者相同时只显示一次。
- 都关闭时仍保留一个占位 logo，避免菜单栏完全消失。

---

## 3. Popover 总览

- 点击菜单栏图标弹出宽 340pt 的窗口。
- 顺序：Codex → Claude Code → Antigravity → Cursor → Command Code（每个服务一个 block，仅显示已启用项，用 Divider 分隔）。
- Header 一行：标题「Usage / 用量」+ 副标题（最近一次刷新相对时间）+ 状态点（live / stale / offline）+ 三个一级图标按钮（刷新 / 统计 / 设置）+ ⋯ kebab 菜单（退出）。
- 每个 block 字段：
  - 服务 tile + 服务名 + 副标题（Provider · Plan）。
  - 主要额度：32pt 剩余百分比 + 16pt `%` + 动态窗口标签 + 进度条 + 重置倒计时；各 Provider 复用既有「今日 cost」+「本周 cost」展示位。
  - 次要额度：存在且与主要额度不重复时显示缩小版进度条；Claude 继续展示 Fable 等模型专项周额度。
  - Cursor：主额度为 Total，次要条展示 Auto 与 API；今日 / 本周费用汇总已成功拉取的远端计量数据，位置和样式与 Codex / Claude 一致；有覆盖或字段缺口时保留当前金额并在后台补拉，完全没有远端快照时才显示既有占位。Unlimited 以 `∞` + `UNLIMITED` 标签表示,不渲染伪造百分比或进度条。
  - Antigravity：主条为 Gemini 5 小时窗口（标签 Gemini 5H），副条 Gemini 周（Gemini WK），辅助行 Claude 5H / Claude WK（按官方 retrieveUserQuotaSummary 分组展示，Gemini 在前）；解析 `loadCodeAssist` + `retrieveUserQuotaSummary`（权威分组）+ `fetchAvailableModels` + `retrieveUserQuota`（兜底分桶），失败保留快照。
  - Command Code：主额度为 5 小时窗口（cap 14.0），次要条展示周窗口（cap 35.0）与 GOAT 月度额度（上限 70 映射）；不展示用量费用。
  - 失败时沿用 block 下方既有短提示；Cursor 与 Command Code 仅显示通用失败状态，不直出远端技术错误原文。
- 关闭全部服务时显示"未启用任何服务 / 到「设置 → 账号」开启"占位。
- 状态点阈值：以当前额度刷新间隔为基准，age ≤ 1.5× 间隔为 live，≤ 3× 为 stale，超过为 offline。

---

## 4. 桌面悬浮窗（Floating HUD）

- 设置里可勾选「显示悬浮窗」。
- 默认按已启用服务逐行展示 Codex、Claude Code、Antigravity、Cursor、Command Code 的主要额度（18pt tile + 进度条 + 大字百分比）。Cursor Unlimited 只显示 `∞`，不显示 100%。
- 行可分别开关，结果只显示一行 / 全部隐藏。
- 样式：14pt 圆角 + `.popover` material + 0.5pt hairline 描边；无标题栏；不抢焦点；可拖动；始终置顶。
- 位置记忆：拖动后写回 UserDefaults；屏幕外重启时回落到默认右上角。
- 拖到距屏幕边缘 20pt 以内自动吸附到该边。

---

## 5. 自动后台刷新

- 后台两条独立循环：
  - 额度 API 循环：默认 2 分钟，可选「1m / 2m / 3m / 5m / 10m」。
  - 本地 JSONL 扫描循环：默认 5 分钟，可选「1m / 2m / 3m / 5m / 10m」；启动后仍立即增量扫描一次，手动刷新不受周期限制。已有用户已保存的间隔保持不变。
- 端点：
  - Codex：`GET https://chatgpt.com/backend-api/wham/usage`，带 `ChatGPT-Account-Id` 头。
  - Claude：`GET https://api.anthropic.com/api/oauth/usage`
  - Antigravity：优先 `POST https://daily-cloudcode-pa.googleapis.com/v1internal:loadCodeAssist` + `retrieveUserQuotaSummary` + `fetchAvailableModels` + `retrieveUserQuota`，主域仅作失败回退（Bearer 直连，token 临期双 client 刷新）；套餐优先读取 `paidTier`，重置时间只使用服务端 `resetTime`。
  - Cursor：`GET https://cursor.com/api/usage-summary`，只携带 Cursor.app 只读登录态构造的 Cookie；401 时最多重读一次本地 SQLite，绝不 OAuth 刷新。
- 限流策略：
  - 同一服务两次成功之间至少 60 秒，周期性触发若间隔不足直接跳过。
  - 收到 429 后该服务进入 10 分钟退避，期间所有请求短路。
  - 手动「立即刷新」可绕过 60s 节流但仍遵守 429 退避。
- 手动刷新会在两种情况下触发一次 Claude CLI 兜底（启动 `claude` 进程，向其 stdin 写入 `/usage`，解析输出）：Claude API 失败且**完全没有可展示缓存**，或 Claude 凭据已过期。CLI 兜底有独立 10 分钟冷却。
- Token 续期：
  - **Codex** 在 access_token 距离过期 5 分钟内用 refresh_token 自动续期，并把新 token 写回原存储位置（`auth.json` 或 cc-bar 自己的 Keychain 条目）。发请求前会重读一次存储，若 `codex` CLI 已自行轮换过就直接采用，不拿陈腐 token 去撞服务端的重用检测。
  - **Antigravity 自动续期**：临期 5 分钟静默用 `https://oauth2.googleapis.com/token` 双 client 刷新并回写 `jetski-standalone-oauth-token`（优先）/`oauth_creds.json`（兜底）。
  - **Claude 不续期**。Anthropic 的 refresh_token 一次性、刷新即轮换，cc-bar 一旦刷新就会把 Claude Code CLI / Desktop 挤下线，因此只读采用它们刷新后的结果。凭据过期时保留上一次额度快照并提示「请打开 Claude Code 刷新登录」，本地 Token / 费用统计不受影响。
- 启动行为：先读本地额度缓存让 UI 立刻有数；启动 7 秒后触发一次额度刷新，之后按调度器节奏轮询；网络或本地服务失败保留已有快照，仅以小字提示。

---

## 6. 本地用量统计

- 数据源：纯本地 JSONL，**额度 API 不返回历史数据**。
  - Claude：`~/.claude/projects/**/*.jsonl`，解析 `type=assistant` 行；`message.usage.speed` 的 `standard` / `fast` 原样映射，缺失或未来新值记为未识别。同一 `message.id` 跨文件全局去重（sidechain / subagent 会重复引用同一条），seen 集合上限 20000。缓存写入原始 Tokens 继续使用 `cache_creation_input_tokens` 合计；计价时读取 `cache_creation.ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens`，分别按当前 Standard / Fast 基础输入价的 1.25× / 2× 计算。
  - Codex：`~/.codex/sessions/**/*.jsonl` + `~/.codex/archived_sessions/**/*.jsonl`，按 `turn_context` 提供的当前 model 给随后的 `event_msg/token_count` 打标签；`thread_settings_applied.thread_settings.service_tier` 的 `priority` / `fast` / `default` 分别映射为 Fast / Fast / Standard，并跨增量扫描保存当前档位。token 用 `payload.info.last_token_usage`，billable input = `input_tokens − cached_input_tokens − cache_write_tokens`。`cache_write_tokens` 兼容顶层及 token details；当前本地日志未提供时按 0，不推测。相同 `total_token_usage` 签名的重复 `token_count` 不再次入账。
  - Pi：`~/.pi/agent/sessions/**/*.jsonl`，解析 `type=message` 的 assistant 行；`message.usage` 直接提供 input / output / cacheRead / cacheWrite / totalTokens。`message.cost.total > 0` 时优先采用日志总价及有效分项；分项与总价只有 JSON 浮点尾差时保留分项并对齐总价。价格缺失或为 0 且有 Token 时，复用统一 Pricing 的在线目录 → 本地价格表补算。compaction / branch_summary 带 `usage` 时一并计入（对齐 pi `/session` 合计口径）。跨文件按 entry id + ISO 时间戳全局去重（fork / clone 复制旧行），seen 上限 20000。模型标签为 `provider/model`，速度档位恒为 Standard。
  - OpenCode：`~/.local/share/opencode/opencode.db`（SQLite，Desktop / CLI 共用，只读打开），解析 `message` 表每行的 `data` JSON；assistant 消息自带 `cost`（USD）与 `tokens{input, output, reasoning, cache{read, write}}`。`cost > 0` 时优先采用日志总价，价格表只用于估算四项费用占比并按日志总价缩放，不改变总费用；价格缺失或为 0 且有 Token 时，复用统一 Pricing 的在线目录 → 本地价格表补算，仍无价格时按 `$0` 展示。reasoning 并入 output。模型标签 `providerID/modelID`（assistant 自带字段优先，缺失时继承会话内最近 user 消息的模型）；会话标题 / 项目路径 / 分支分别来自 `session` / `workspace` 表，空标题用首条 text part 兜底。增量 watermark 为 `max(message.time_created)`（Unix 毫秒），按 message.id 全局去重，seen 上限 20000。
- **Pi / OpenCode 无订阅额度**：不进入额度轮询、Popover 额度区、菜单栏、悬浮窗与 Timeline；仅在主窗口用量统计与对话页展示（两者今日花费仍计入 Popover 今日合计）。
- 增量逻辑：每个文件按 `mtime + offset` 维护 watermark，未变化直接跳过；文件被截断时 offset 归零重扫，去重由全局 seen 兜底。
- 聚合：按 `(day, app, model, speed)` 在内存中累加；持久化到 `usage-rollup.json`。总 Tokens 永远使用日志原始值，Fast 的计费等效 Tokens 按独立倍率另算，不能回写或放大总 Tokens。
- 对话聚合：同一批扫描同时按 `(conversation, day, model, speed)` 聚合并写入 `conversation-rollup.json`，不保存聊天正文。Codex 以 `session_meta.id` 为对话 ID，标题优先取 `~/.codex/session_index.jsonl`；Claude 以 `sessionId` 为对话 ID，相同 sessionId 的 subagent / sidechain 合并到主对话，标题优先取 `~/.claude/history.jsonl`；Pi 以 `type=session` 的会话 id 为对话 ID（缺失时兜底文件名 UUID）；OpenCode 以 `session.id` 为对话 ID（key 前缀 `opencode:`），标题取 `session` 表的 title。
- 对话项目归属：以规范化完整路径作为项目身份；存在的路径优先归一到 Git 根目录，Claude 同时参考 history project、主会话 cwd 与顶层项目容器，子任务目录不能覆盖主项目。已删除项目保留历史归属；用户主目录 / 根目录归为“无明确项目”，CCBar probe / wakeup 归为“CCBar 系统任务”。同名不同路径不合并。
- 对话费用是按模型 API 公开价格折算的**等值估算**，不是 Codex / Claude 订阅实际账单；无法取得可靠价格时，该部分费用按 `$0` 参与展示，不追加“未定价”标记，Tokens 和请求数仍保留。
- Fast 价格优先使用带明确档位语义的在线字段（LiteLLM 的 OpenAI Priority、models.dev 官方 provider 的 `experimental.modes.fast.cost`），命中不到再回落 `Pricing.swift` 的可指纹化本地规则；绝不把 Standard 价格当 Fast。Codex 按 Priority API 等值费率估算，ChatGPT 信用消耗倍率仍只由本地可靠规则提供；Claude 仅在 Standard/Fast 四项费率比例完全一致时允许推导等效倍率。未收录 Fast 模型以及完整输入 `>272K` 的 Codex Fast 请求保留原始 Tokens、档位和请求数，无法计算的费用按 `$0` 展示；未收录倍率时计费等效 Tokens 为 0、倍率显示 `—`。
- 价格表：GPT-5.6 与 GPT-5.5 阶梯价（完整输入 `>272,000` 时该次请求全部用长上下文价）、GPT-5.5 Pro 固定本地价（输入 / 缓存读取 / 输出为 `$30 / $30 / $180`，无缓存折扣）、Claude Sonnet 5 分段生效价等少数需要特殊规则的模型继续手工维护在 `Core/Usage/Pricing.swift`，不受远端数据影响。Claude Opus 4.6/4.7 Fast 价格仅保留用于历史日志计价。Claude 缓存写入缺少 TTL 明细时全部按默认 5m 估算；Codex 缓存写入字段本地日志未提供时费用估算为 0，不推测。其余 Standard / Fast 模型由内置价格表 + 后台自动拉取的 LiteLLM / models.dev 远端价格目录共同提供：常规 24 小时检查不变；扫描发现新的缺价 Standard/Fast 模型时立即绕过 24 小时刷新，同一 app/model/speed 30 分钟内最多尝试一次；设置页也可手动更新。新价格只在扫描安全边界提交，并自动触发相关历史用量重算。无网络或远端不可用时保留旧快照并降级为内置表，不影响离线使用。命中不到任何价格源时 token 仍记录、费用按 0 汇总。模型名归一化：循环剥 `openai-codex/`、`openai/`、`anthropic/`、`deepseek/`、`opencode-go/`、`commandcode/`、`command-code/` 前缀（Command Code 网关标签可能是 `commandcode/deepseek/...` 双层，剥完外层需继续剥内层），以及 `@YYYYMMDD` 或 `-YYYY-MM-DD` 日期后缀，本地与远端目录共用同一套规则。

---

## 7. 主窗口 — 统计

- 主窗口为普通 macOS 窗口，最小 1040×520，含顶部 TabView：统计 / 设置。
- 入口：Popover 的统计 / 设置图标、菜单栏的 ⌘1 / ⌘, 命令；从 Dock、应用程序、Launchpad 或 Raycast 启动 / 重新打开 CCBar 时，主窗口置前并固定切到统计 tab；⌘R 触发立即刷新。开机自动启动保持静默，首次启动仍先展示 Onboarding。
- 统计 tab：
  - 侧栏：服务过滤（All / Codex / Claude Code / Cursor / Pi / OpenCode，仅列出设置中启用的服务），View 切换（Overview / Conversations / Cycles / Timeline）。Cursor 只进入 Overview；进入 Conversations 时自动回退 All，Cycles 只统计 Codex / Claude 主账号，但保留全部服务筛选项；筛选选择在 Cycles 中不改变内容，切回其他视图后继续生效。
  - 顶部两级 segmented Picker：**粒度（日 / 周 / 月）是上级**，决定「一个统计单元」是一天、一周还是一个月；**时间范围是下级**，选项随粒度切换——日：Today / Yesterday / 7d / 30d / This week / This month / Year / All / Custom（日粒度额外保留三档自然周期，用于按天分桶只统计本周 / 本月 / 本年）；周：This week / Last week / 4w / 12w / All / Custom；月：This month / Last month / 6m / Year / All / Custom。整页所有面板（KPI、Token 拆分、按服务、按模型、按提供商、用量柱状图）都按所选范围统计，柱状图按粒度分桶。切换粒度时范围自动收敛到新粒度的第一档，All / Custom 三个粒度共用、切换时保留。周范围与周分桶的周起点统一为周一，月按自然月；范围边界处被截断的不完整周 / 月不补全、不标注。粒度与范围在 Overview 和 Conversations 之间共享。
  - 自定义：两个 DatePicker（仅在 Custom 时出现）。
  - KPI：总 tokens / 总花费 / 各可见服务的花费卡（Codex / Claude Code / Cursor / Pi / OpenCode 按「统计服务」配置增减）一行排列；每张带相对上一同长度区间的 delta 百分比。Cursor 的 `chargedCents` 仅作为内部费用数据源；任一 Cursor 桶费用不完整时，其服务卡、总花费、模型和每日 tooltip 都显示 `--`，不展示部分金额或额外口径说明。
  - Token 拆分：保留原始总 Tokens 口径，另列 Fast 原始 Tokens、计费等效 Tokens、Fast 估算费用和占比；按服务、按模型行提供紧凑 Fast 明细。总花费及 Popover 今日/本周花费自动包含可定价的 Fast 溢价，Popover 不增加字段。
  - 用量趋势：堆叠柱状图（各可见服务），用 SwiftUI Charts，柱子按顶部粒度为日 / 周 / 月；标题随粒度显示每日 / 每周 / 每月用量。所选范围只覆盖一个周期（日 · 今天 / 昨天，周 · 本周 / 上周，月 · 本月 / 上月，以及不跨周期的自定义）时，图表自动扩展为近 14 个周期的上下文，所选周期全彩、之前的周期降透明，KPI 与其他面板口径不变。Cursor 选择有限日期范围时，未覆盖的历史按月静默补拉；All 只使用现有缓存，不做无界回溯，也不新增加载、覆盖范围或不完整提示。
  - 服务占比：水平条形 + KPI 文本；可见服务 ≤ 3 时单行排列，超过 3 个（全开时）两列换行、每行两个对齐铺满。
  - 当前额度语义由统一 `QuotaSnapshot` 提供；紧凑入口展示主要额度，详细入口按主/次/模型专项额度展示，颜色按状态色逻辑。
  - 按提供商：跨服务按模型厂商归并（OpenAI / Anthropic / DeepSeek / OpenCode-Go / Command Code / 其他），面板置于概览页最下方，所有有数据的提供商一行一个平铺展示 Tokens 与费用，右上角按费用 / Tokens / 请求数 / 名称四档排序（选哪档按哪档排、数值大的在前，「其他」恒排最后，展开的模型明细跟随同一排序键）；点击行就地展开该提供商的 Token 拆分（入 / 出 / 缓存命中 / 命中率）、来源服务标注与该提供商下按模型明细。归属规则：前缀优先（`openai/`、`openai-codex/` → OpenAI，`anthropic/` → Anthropic，`deepseek/` → DeepSeek，`opencode-go/` → OpenCode-Go 单列，便于区分 opencode 内花的钱，`commandcode/`、`command-code/` → Command Code 单列，便于区分 Command Code 会员/网关内花的钱），无前缀时按模型名关键词 / app 兜底（Codex → OpenAI、Claude → Anthropic），其余归「其他」。纯展示层推导，不改动定价与存储。
  - 按模型明细：分服务列出 model · 调用次数估算 · tokens · cost。
  - Conversations：左侧列表按所选时间范围展示对话标题、项目、服务、模型、请求数、Tokens 与 API 等值估算费用；全 Fast 对话显示 `Fast` 徽标，部分 Fast 对话显示 `Mixed`，纯 Standard 和仅未识别档位不加列表噪声。支持 Codex / Claude / Pi / OpenCode 服务筛选、单项目筛选、标题/项目搜索、最近/Tokens/费用排序和每次 200 条渐进加载；Cursor Dashboard 没有本机会话 ID，不进入本页。项目菜单区分正常（含受保护目录下未验证存在的项目）、已不存在、无明确项目和 CCBar 系统任务；为避免触发系统文件夹授权弹窗，桌面/文稿/下载/音乐/图片/影片等保护目录下的路径不做文件系统检查，仅按日志中的路径字符串归组。右侧详情固定展示该对话全部时间的四项 Token、缓存命中率、各速度档位的请求数/原始 Tokens、Fast 计费等效 Tokens、Standard/Fast 费用、四项最终费用、模型构成与完整时间信息。对话页刷新只扫描本地日志，不等待额度或服务状态网络请求。
  - Timeline：5H 固定展示今天（本地 00:00–24:00）的主要额度数据；周视图按服务返回的额度重置时刻展示当前和上一额度周期，不按自然周划分。图表同时包含真实变动与当前周期最新采样，跨周期重置不伪造成消耗事件。每个账号独立分区，顺序为 Codex 主账号 → Codex 副账号 → Claude Code。
  - Cycles：按 Codex / Claude 主账号真实重置窗口统计当前周周期与 5 小时周期的本机实际 Tokens、API 等值费用，并根据本机观察到的官方比例增量计算用满预估。页面同时展示 Codex / Claude × 5 小时 / 周四张 KPI 卡，按服务分成两行、每行两张等宽卡，官方实时百分比继续表达当前额度状态；不展示已结束周期、历史曲线或历史筛选。统计从功能首次启用后开始，不反推此前周期，不读取日聚合或 Claude 补录数据；账号切换按时间段隔离，Claude 用邮箱脱敏哈希区分主账号。预估仅为本机 API 等值推算，不代表官方绝对 Token / credit 上限。
  - 本地日志读取、周期重建或统计持久化真实失败时，统计页顶部显示警告，保留仍可用的已有数据，并提供“打开设置”入口让用户触发重新计算；扫描过程中会话文件被正常归档或清理不算失败、不提示误报警告，错误恢复后警告自动消失。
  - 统计服务显示配置：可在「设置 → 统计服务」勾选要计入统计的服务（Codex / Claude Code / Pi / OpenCode 默认开启，Cursor 默认关闭）。关闭后该服务从侧栏过滤项、KPI 卡、Token 拆分、按服务、按提供商、按模型和每日用量图表中一并消失，占比分母只统计可见服务（归一化）；全部关闭时「按服务」面板显示空态引导。Cursor 统计开关独立于额度卡片，但启用任一入口都会启动其只读远端刷新；本地服务关闭后仍会被扫描与入库，重新开启后历史数据照常出现。

---

## 8. 设置

| 分类 | 项 |
|---|---|
| 账号 | 显示 Codex、Claude Code、Antigravity、Cursor、Command Code（自动检测的邮箱 / 用户名 / plan 显示在副标题；未检测到时统一使用既有 Provider 状态；Command Code 支持配置凭据来源：自动扫描 / Keychain 手动 Key） |
| 统计服务 | 勾选要计入用量统计的服务（Codex / Claude Code / Pi / OpenCode 默认开启，Cursor 默认关闭）；Cursor 统计与额度卡片独立，启用任一入口才开始只读远端刷新 |
| 菜单栏 | 分服务开关（Codex / Claude Code / Antigravity / Cursor / Command Code）、额度周期（主要 / 周 / 两者；旧 5H 设置自动迁移为主要） |
| 桌面悬浮窗 | 显示悬浮窗、分服务行开关（Codex / Claude Code / Antigravity / Cursor / Command Code） |
| 刷新 | 额度刷新间隔、日志扫描间隔、重置时间显示（剩余时长 / 具体时间）、上次刷新（只读）、手动更新价格目录、重新计算用量 |
| 通用 | 语言（跟随系统 / 中文 / English）、隐私模式（Popover 中隐藏主账号邮箱、Codex 副账号名称）、开机自动启动（SMAppService）、版本（只读） |

切换额度 / 日志间隔后会立即同步到 Scheduler，无需重启。

---

## 9. 首次启动引导（Onboarding）

- 首次启动检测到 `SettingsStore.didCompleteOnboarding == false` 时弹出 4 步窗口（620×520，`.regularMaterial`）：
  1. Welcome：标题 + 简介。
  2. Detect accounts：展示自动识别的 Codex / Claude Code / Antigravity / Cursor / Command Code 账号；未检测到时统一显示既有状态，检测全程只读。
  3. Configure：菜单栏窗口 / 悬浮窗开关等关键设置一次性勾选。
  4. Ready：完成，按钮可直接进入"统计"或关闭。
- 完成后写入 `didCompleteOnboarding=true`，后续启动不再展示。

---

## 10. 双语

- 全局 `tr(en, zh)` 选词，由 `SettingsStore.resolvedLanguage` 决定渲染。
- `跟随系统` 看 `Locale.preferredLanguages.first`，以 `zh` 开头判定为中文（兼容简繁）。

---

## 11. 持久化文件清单

| 位置 | 文件 | 内容 |
|---|---|---|
| `~/Library/Application Support/CCBar/` | `quota-cache.json` | 额度快照 |
| `~/Library/Application Support/CCBar/` | `quota-history-today.json` | 今日主要额度变动事件（含真实窗口类型） |
| `~/Library/Application Support/CCBar/` | `quota-cycle-history.json` (v4) | 功能启用后 Codex / Claude 主账号 5H / WK 周期、额度段、重置采样与数据质量；载入时幂等清理重叠记录与 resetAt 漂移残片，v2 / v3 自动迁移 |
| `~/Library/Application Support/CCBar/` | `usage-rollup.json` (v9) | (day, app, model, speed) 聚合桶 |
| `~/Library/Application Support/CCBar/` | `conversation-rollup.json` (v6) | 对话档案与 (conversation, day, model, speed) 聚合桶 |
| `~/Library/Application Support/CCBar/` | `cycle-usage-rollup.json` (v4) | 功能启用后的 (cycle, allowance segment, app, model, speed, quality) 周期用量桶；旧版派生数据升级后自动重建 |
| `~/Library/Application Support/CCBar/` | `pricing-catalog.json` (v2) | LiteLLM / models.dev Standard/Fast 远端价格目录缓存（含各源 etag / 拉取时间 / 失败时间与缺价刷新冷却） |
| `~/Library/Caches/CCBar/` | `scan-state.json` (v12) | 每个 JSONL 文件的 watermark、Codex 当前模型/速度/累计签名、Claude 跨文件 seen ID 集、Pi watermark 与跨文件 seen 集、OpenCode message watermark 与跨会话 seen ID 集 |
| `UserDefaults` | `ccbar.settings.*` | 所有设置项 + 悬浮窗 frame + 引导/Keychain 弹窗标记 |

四份扫描相关缓存（scan-state 与三份 rollup）共享 generationID；结构版本或 generationID 不一致会丢弃旧桶并触发一次全量重扫。价格变化不触发自动重扫：自动扫描按当前价格计新条目，历史费用保持旧价，由用户在设置页手动全量重算——「重新计算用量」是唯一的全量重算入口（缺价补齐也只发生在这一步）；「更新价格目录」只拉取价格表，不触发重算。

---

## 12. 一期明确不做

- 不做 App 内 OAuth 登录。
- 不做浏览器 Cookie 自动导入。
- 不做 WebView 抓 dashboard DOM。
- 不做 Copilot / 国产 Token Plan。
- 不上 Mac App Store（沙盒会拦住 `~/.claude` / `~/.codex` 读取）。
- 不做组织级 Admin API。
- 不做额度告警通知。

`claude /usage` 的 CLI 兜底当时被列为「一期不做」，实际作为最后一道兜底已经做了；只在 Claude API 失败且无可展示缓存的手动刷新场景启用，不替代主路径。

---

## 13. 风险 / 待持续关注

1. **非公开接口稳定性**：`wham/usage` / `oauth/usage` 字段可能随时调整，解析器需要保持容错。
2. **Keychain 弹窗**：首次读 `Claude Code-credentials` 系统会弹一次授权窗，引导文案已加。
3. **价格表漂移**：当前对齐 cc-switch / CodexBar 2026 上半年价位；模型迭代后需要同步。
4. **JSONL 量级**：长期使用后 `~/.claude/projects` 可达数百 MB；增量 watermark + seen ID 上限保证启动 / 扫描时间可控，但 seen 集合上限 20000 时若历史窗口超过该长度会有边缘重复风险。
5. **签名与公证**：发布前需 Apple Developer 证书；当前开发期未签名，README 标注「右键打开」。
