本版解决只用 Claude Desktop、不常开终端的用户被反复提示「凭据已过期」的问题。

**新增**
- 没装或从没用过 Claude Code 命令行时，也能直接从 Claude Desktop 识别 Claude 账号并显示额度，不再提示未配置

**修复**
- Claude Code 的登录凭据只有 8 小时有效期，改用 Claude Desktop 聊天时不会续期，隔夜就提示「凭据已过期，请登录」且额度停在旧数字；现在会自动改用 Claude Desktop 的登录状态继续取数
- 凭据来源切换时可能被误判成换了账号，导致已有的额度快照和历史时间线被清空
